// gradle plug-in for eclipse update site
http://dist.springsource.com/release/TOOLS/gradle

// build child project (from mdw-framework dir)
gradle mdw-common:build

// show dependency graph for a project (from mdw-framework dir)
gradle :mdw-common:dependencies

// specify a build file and task
gradle -b test.gradle publishTestResults

// use specific Java for gradle builds in Eclipse
Window > Preferences > Gradle > Arguments > Workspace JRE

// Gradle Dependencies source attachment - not able to view source
1) Try deleting the corresponding dependencies from Gradle cache (C:\Users\aa72442\.gradle) and then refresh Gradle dependencies
2) If step1 doesn't resolve the issue try deleting the Maven cache from affected bundles.

// gradle ide eclipse plugin fails to perform subproject builds 
// ("Project with path 'x' could not be found in root project 'y')
1) Window > Preferences > Gradle > Arguments > Program Arguments"
   -c "../mdw-framework/settings.gradle"
2) Or for project-specific values, add the setting above to the External Tool launch config 

Buildship flat subproject bug:
https://bugs.eclipse.org/bugs/show_bug.cgi?id=476070
Older Gradle IDE bug:
https://issuetracker.springsource.com/browse/STS-3892