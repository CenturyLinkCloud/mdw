DB Types Supported
  Oracle
  MYSQL
  
Under Oracle there are two sets of SQL scripts
  RDBMS
  VCSBasedDB
  
  There is also a BAM folder which can be used for either RDBMS of VCS if teams are using BAM functionality
  oldScripts folder contains the scripts which are based on from MDW 5.2 and MDW 4.5 they are just there for reference purpose only. Do not use the scripts under this folder.  
  