  // manual task and event wait
start process("TaskEventTest") {
  variables = [ path: "AutoForm" ]
}
sleep 10
notify event("C-${masterRequestId}") {
    message = "Test"
}
sleep 40
action task("AutoForm Task with Non-Recurring Event") {
    outcome = "Claim"
}
sleep 15
action task("AutoForm Task with Non-Recurring Event") {
    outcome = "Complete"
}
sleep 15
verify process("TaskEventTest")
