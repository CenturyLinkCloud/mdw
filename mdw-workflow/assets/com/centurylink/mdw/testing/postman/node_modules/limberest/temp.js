'use strict';

//var codes = require('builtin-status-codes')

//console.log("STATUS: " + codes[404]);

var itemVals = {
  "group-name": "GroupB"
};

var envVals = {
  "base-url": "http://localhost:8080/mdw/Services",
  "group-name": "GroupA"
}

var vals = Object.assign({}, envVals, itemVals);

console.log("VALS: " + JSON.stringify(vals, null, 2));